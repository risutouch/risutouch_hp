/* ================================================
   りすたっち — サイト設定
   ここの数値を変えると各パーツの動作が変わります
   ================================================ */

window.__siteConfig = {

  // ── メインビジュアル（背景スライド） ──────────────
  hero: {
    displayMs:   12000,  // 1枚あたりの表示時間（ms）
    fadeMs:       2000,  // スライド間フェード時間（ms）
    colDelayMs:   1500,  // 縦書き2列目が出るまでの間隔（ms）
  },

  // ── 商品吹き出し（ヒーロー右下ポップアップ） ──────
  heroBalloon: {
    cycleMs:      8000,  // 吹き出しメッセージの切り替え間隔（ms）
  },

  // ── 店舗写真スライドショー ────────────────────────
  shopSlide: {
    intervalMs:   6000,  // 写真切り替え間隔（ms）
  },

  // ── 商品カルーセル ────────────────────────────────
  productCarousel: {
    autoMs:       3500,  // 自動スクロール間隔（ms）
  },

  // ── ミツバチ ──────────────────────────────────────
  bee: {
    firstDelayMin:    3000,  // 初回登場までの最短待機（ms）
    firstDelayRange:  5000,  // 初回登場のランダム幅（ms）
    intervalMin:     12000,  // 次回登場までの最短待機（ms）
    intervalRange:   20000,  // 次回登場のランダム幅（ms）
    speedMin:         0.08,  // 飛行速度の最小値（px/ms）
    speedRange:       0.04,  // 飛行速度のランダム幅（px/ms）
    speedMinMobile:   0.05,  // モバイル飛行速度の最小値（px/ms）
    speedRangeMobile: 0.02,  // モバイル飛行速度のランダム幅（px/ms）
    fleeSpeed:        0.28,  // 逃走速度（px/ms）
  },

  // ── お知らせバブル ────────────────────────────────
  notice: {
    showDelayMs:  5000,  // ページ読み込みからバブル表示までの遅延（ms）
  },

};
